// you are given the head pointers of two singly linked lists that eventually merge at a common node. After this merge point, both lists share all subsequent nodes (i.e., they reference the same memory locations).
// Tour task is to find the node where the two lists merge and return the value stored at that node.
// Output Format
// - Return the data value of the node where the two linked lists merge.
// - if no merge point exists, return none or -1.
// Example:
// List A: 1->2->3->
//                               6->7->8
// List B: 4->5->

// Output: 6 